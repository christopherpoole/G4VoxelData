if(@WITH_DICOM@ MATCHES "ON")
    set(G4VOXELDATA_DICOM_LIBRARIES gdcmMSFF)
    set(G4VOXELDATA_DICOM_USE_FILE @G4VOXELDATA_DICOM_USE_FILE@)
    find_package(GDCM REQUIRED)
    include(${GDCM_USE_FILE})
endif()

if(@WITH_NUMPY@ MATCHES "ON")
    set(G4VOXELDATA_NUMPY_LIBRARIES @G4VOXELDATA_NUMPY_LIBRARIES@)
    set(G4VOXELDATA_NUMPY_USE_FILE)
endif()

include_directories(@G4VOXELDATA_INCLUDE_DIRECTORY@)


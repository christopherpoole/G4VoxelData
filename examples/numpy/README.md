This example shows how to load a Python `numpy` array from a file as G4VoxelData

# Usage

    $> mkdir build
    $> cd build/
    $> cmake ..
    $> make
    $> cd ..
    $> ./NumpyExampe data/data.npy

